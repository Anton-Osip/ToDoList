import {TasksState} from "../App";

export const tasksReducer = (state: TasksState, action: any): TasksState => {
    switch (action.type) {
        case 'XXX':
            return state
        default:
            return state
    }
}
